# Botpress.CLI

Command-line interface for the Botpress SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Botpress.CLI --prerelease
```

## Usage

```bash
botpress --help
botpress api --help
```